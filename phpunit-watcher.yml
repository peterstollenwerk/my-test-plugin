watch:
  directories:
    - src
    - tests
    - classes
  fileMask: '*.php'
notifications:
  passingTests: false
  failingTests: true
# phpunit:
#   binaryPath: vendor/bin/phpunit
#   arguments: '--stop-on-failure'
#   timeout: 180